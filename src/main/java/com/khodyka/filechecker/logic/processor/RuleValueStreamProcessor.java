package com.khodyka.filechecker.logic.processor;

import java.util.stream.Stream;

public class RuleValueStreamProcessor implements RuleValueProcessor<Stream<String>> {

    @Override
    public void process(Stream<String> stream) {
        stream
                .
    }
}
